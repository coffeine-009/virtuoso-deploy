Virtuoso deployment
===============================================
Deployment configurations for virtuoso.

Docker-compose
-----------------------------------------------
Docker compose configuration for deploying virtuoso.

```bash
docker-compose up -d
```
